{
  "name": "changelog-generator",
  "version": "0.2.2",
  "description": "A GitHub Action that compares the commit differences between two branches",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build ./index.js",
    "precommit": "npm run build && git add dist/",
    "bump:readme": "replace 'v[0-9]+.[1-9]+.[0-9]+' \"v$npm_package_version\" ./README.md",
    "bump:workflow": "replace 'v[0-9]+.[1-9]+.[0-9]+' \"v$npm_package_version\" ./.github/workflows/*yml",
    "version": "npm run bump:readme && npm run bump:workflow && git add ./dist ./README.md ./.github/workflows/*yml",
    "postversion": "git push && git push --tags",
    "precommit-msg": "echo 'Pre-commit checks...' && exit 0",
    "lint": "eslint ./",
    "format": "prettier --write **/*.{js,yml,json}",
    "format-check": "prettier --check **/*.{js,yml,json}"
  },
  "pre-commit": [
    "precommit-msg",
    "lint",
    "precommit"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/metcalfc/changelog-generator.git"
  },
  "keywords": [
    "releases",
    "commits",
    "commit",
    "difference",
    "branching",
    "branch",
    "difference"
  ],
  "author": "Chad Metcalf",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/metcalfc/changelog-generator/issues"
  },
  "homepage": "https://github.com/metcalfc/changelog-generator#readme",
  "dependencies": {
    "@actions/core": "^1.2.3",
    "@actions/exec": "^1.0.3",
    "@actions/github": "^2.1.1"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.22.0",
    "eslint": "^6.8.0",
    "isexe": "^2.0.0",
    "pre-commit": "^1.2.2",
    "prettier": "^2.0.2",
    "replace": "^1.1.5"
  }
}
